job:
  chdir: false

run:
  dir: ${paths.run_dir_root}/${task_name}/runs/${now:%Y-%m-%d_%H-%M-%S}

sweep:
  dir: ${paths.run_dir_root}/${task_name}/multiruns/${now:%Y-%m-%d_%H-%M-%S}
  subdir: ${hydra.job.num}
