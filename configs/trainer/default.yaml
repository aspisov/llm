_target_: lightning.pytorch.Trainer
max_steps: ${training.num_iterations}
val_check_interval: ${training.val_frequency}
limit_val_batches: 10
devices: 1
fast_dev_run: false
gradient_clip_val: ${training.max_l2_norm}
gradient_clip_algorithm: norm
