name: midtrain
backend: lightning
objective: continued_pretraining
required_config_groups: [data, module, optimizer, scheduler]
runner:
  _target_: llm_scratch.train.runners.stage_runners.run_lightning_stage
