name: rlvr
backend: torch
objective: reinforcement_learning_with_verifiable_rewards
required_config_groups: [data, module, optimizer, scheduler]
runner:
  _target_: llm_scratch.train.runners.stage_runners.run_torch_stage
