_target_: llm_scratch.train.factories.build_transformer
vocab_size: 10000
context_length: 256
num_layers: 4
d_model: 512
num_heads: 32
d_ff: 1344
rope_theta: 10000
device: ${hardware.device}
dtype: ${hardware.dtype}
